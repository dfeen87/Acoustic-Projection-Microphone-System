import React from "react";
import ReactDOM from "react-dom/client";
import APMDashboard from "./APMDashboard.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <APMDashboard />
  </React.StrictMode>
);
